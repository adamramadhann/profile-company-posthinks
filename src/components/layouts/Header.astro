---
// src/components/layouts/Header.astro
// Modern Navbar with Cyan/Teal Theme
import { getTranslations, getAlternateUrl, type Locale } from '../../lib/i18n-data';

interface Props {
  currentLang: Locale;
}

const { currentLang } = Astro.props;

// Load translations
const t = await getTranslations(currentLang);

// Build navigation links with correct paths - ALL routes have prefix
const navLinks = [
  { href: `/${currentLang}`, label: t.nav.home },
  { href: `/${currentLang}/solutions`, label: t.nav.solutions },
  { href: `/${currentLang}/community`, label: t.nav.community },
  { href: `/${currentLang}/case-studies`, label: t.nav.caseStudies },
  // { href: `/${currentLang}/clients`, label: t.nav.clients },
  { href: `/${currentLang}/contact`, label: t.nav.contact },
];

// Language switcher URLs
const currentPath = Astro.url.pathname;
const idUrl = getAlternateUrl(currentPath, 'id');
const enUrl = getAlternateUrl(currentPath, 'en');

// WhatsApp URL for consultation
const whatsappNumber = '6281234567890';
const whatsappMessage = currentLang === 'id'
  ? 'Halo Posthinks! Saya tertarik untuk konsultasi tentang layanan digital marketing.'
  : 'Hello Posthinks! I am interested in consulting about digital marketing services.';
const whatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(whatsappMessage)}`;

// Determine active link
const getIsActive = (href: string) => {
  const pathWithoutLocale = currentPath.replace(/^\/(en|id)/, '') || '/';
  const hrefWithoutLocale = href.replace(/^\/(en|id)/, '') || '/';
  return pathWithoutLocale === hrefWithoutLocale;
};
---

<!-- Header dengan Cyan/Teal Theme - Solid Dark with Blur -->
<header id="navbar" class="sticky top-0 z-50 w-full bg-slate-950/90 backdrop-blur-xl border-b border-gradient-to-r from-cyan-500/30 via-teal-500/30 to-cyan-500/30 transition-all duration-300 relative">
  <nav class="container mx-auto px-3 sm:px-6 lg:px-8" aria-label="Main navigation">
    <div class="flex h-16 sm:h-20 items-center justify-between">
      <!-- Logo - Posthinks Logo Only -->
      <a href={`/${currentLang}`} class="flex items-center group" aria-label="Posthinks Home">
        <div class="relative flex items-center justify-center transition-transform duration-300">
          <!-- Posthinks Logo Image - Responsive size -->
          <img
            src="/logo-posthinks.png"
            alt="Posthinks Logo"
            class="h-10 sm:h-12 lg:h-14 w-auto object-contain transform group-hover:scale-105 transition-transform duration-300"
            width="160"
            height="45"
          />

          <!-- Subtle Glow Effect on Hover -->
          <div class="absolute inset-0 rounded-xl bg-gradient-to-br from-cyan-500/20 to-teal-500/20 opacity-0 group-hover:opacity-100 blur-xl transition-opacity duration-300"></div>
        </div>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden lg:flex lg:items-center lg:space-x-1">
        {
          navLinks.map((link) => {
            const isActive = getIsActive(link.href);
            return (
              <a
                href={link.href}
                class={`relative px-4 py-2 text-sm font-medium rounded-lg transition-all duration-300
                  ${isActive
                    ? 'text-cyan-400 bg-cyan-950/50 border border-cyan-500/30'
                    : 'text-gray-300 hover:text-cyan-400 hover:bg-cyan-950/30'
                  }
                  animate-fade-in`}
                style={`animation-delay: ${navLinks.indexOf(link) * 50}ms`}
                aria-current={isActive ? 'page' : undefined}
              >
                {link.label}

                <!-- Active Indicator -->
                {isActive && (
                  <span class="absolute bottom-1 left-1/2 transform -translate-x-1/2 w-8 h-0.5 bg-gradient-to-r from-cyan-500 to-teal-500 rounded-full"></span>
                )}
              </a>
            );
          })
        }

        <!-- Language Switcher - Hover Modal -->
        <div class="relative border-l border-white/10 pl-6 ml-2 lang-switcher">
          <!-- Trigger Button -->
          <button
            type="button"
            class="flex items-center gap-2 px-4 py-2 bg-slate-900/80 border-2 border-white/10 rounded-xl text-sm font-bold text-white hover:border-cyan-500/50 transition-all duration-300"
            aria-label="Change language"
          >
            <span class="text-base">{currentLang === 'id' ? '🇮🇩' : '🇬🇧'}</span>
            <span class="font-black tracking-wide">{currentLang === 'id' ? 'ID' : 'EN'}</span>
            <svg class="w-4 h-4 text-cyan-400 transition-transform duration-300 lang-arrow" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>

          <!-- Hover Modal -->
          <div class="absolute right-0 top-full pt-3 w-36 opacity-0 invisible lang-modal transition-all duration-300 transform translate-y-2 z-50 pointer-events-none">
            <!-- Modal Container -->
            <div class="bg-slate-900/95 backdrop-blur-xl border-2 border-white/10 rounded-2xl shadow-2xl shadow-black/50 overflow-hidden pointer-events-auto">
              <!-- Modal Header -->
              <div class="px-4 py-3 border-b border-white/10">
                <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider">
                  {currentLang === 'id' ? 'Pilih Bahasa' : 'Select Language'}
                </p>
              </div>

              <!-- Language Options -->
              <div class="p-2">
                <!-- Indonesian Option -->
                <a
                  href={idUrl}
                  class={`group/item flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-300 ${
                    currentLang === 'id'
                      ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border border-cyan-500/30'
                      : 'hover:bg-white/5 border border-transparent'
                  }`}
                >
                  <!-- <span class="text-2xl">🇮🇩</span>z -->
                  <div class="flex-1">
                    <!-- <p class={`text-sm font-bold ${currentLang === 'id' ? 'text-cyan-400' : 'text-white'}`}>
                      Bahasa Indonesia
                    </p> -->
                    <p class="text-xs text-gray-400">ID</p>
                  </div>
                  {currentLang === 'id' && (
                    <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                  )}
                </a>

                <!-- English Option -->
                <a
                  href={enUrl}
                  class={`group/item flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-300 ${
                    currentLang === 'en'
                      ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border border-cyan-500/30'
                      : 'hover:bg-white/5 border border-transparent'
                  }`}
                >
                  <span class="text-2xl">🇬🇧</span>
                  <div class="flex-1">
                    <!-- <p class={`text-sm font-bold ${currentLang === 'en' ? 'text-cyan-400' : 'text-white'}`}>
                      English
                    </p> -->
                    <!-- <p class="text-xs text-gray-400">EN</p> -->
                  </div>
                  {currentLang === 'en' && (
                    <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                  )}
                </a>
              </div>
            </div>

            <!-- Arrow Pointer -->
            <div class="absolute -top-0 right-6 w-4 h-4 bg-slate-900 border-l-2 border-t-2 border-white/10 transform rotate-45 translate-y-2"></div>
          </div>
        </div>

        <!-- CTA Button - WhatsApp -->
        <a
          href={whatsappUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="group relative inline-flex items-center justify-center rounded-xl
                 px-5 py-2.5 text-sm font-bold
                 bg-gradient-to-r from-green-500 via-green-600 to-emerald-600
                 text-white shadow-lg shadow-green-500/25
                 transition-all duration-300
                 hover:shadow-xl hover:shadow-green-500/40 hover:scale-105
                 overflow-hidden
                 animate-fade-in ml-4"
          style="animation-delay: 250ms"
        >
          <!-- Background Animation on Hover -->
          <div class="absolute inset-0 bg-gradient-to-r from-green-600 via-green-700 to-emerald-700 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>

          <!-- Shimmer Effect -->
          <div class="absolute inset-0 -translate-x-full group-hover:animate-shimmer bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>

          <span class="relative z-10 flex items-center gap-2">
            <!-- WhatsApp Icon -->
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
            </svg>
            {t.nav.consultation}
          </span>
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        class="lg:hidden inline-flex items-center justify-center rounded-xl p-2.5
               text-gray-300
               bg-white/5 border border-white/10
               hover:bg-cyan-950/30
               hover:text-cyan-400 hover:border-cyan-500/30
               transition-all duration-300
               focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-500 focus-visible:ring-offset-2"
        aria-expanded="false"
        aria-label="Toggle menu"
        id="mobile-menu-button"
      >
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <!-- Mobile Navigation -->
    <div
      class="lg:hidden hidden"
      id="mobile-menu"
      role="menu"
      aria-orientation="vertical"
      aria-labelledby="mobile-menu-button"
    >
      <div class="space-y-2 pb-6 pt-4">
        {
          navLinks.map((link) => {
            const isActive = getIsActive(link.href);
            return (
              <a
                href={link.href}
                class={`block rounded-xl px-4 py-3 text-base font-medium transition-all duration-300
                  ${isActive
                    ? 'text-cyan-400 bg-cyan-950/50 border border-cyan-500/30'
                    : 'text-gray-300 bg-white/5 border border-white/10 hover:bg-cyan-950/30 hover:text-cyan-400 hover:border-cyan-500/30'
                  }
                  role="menuitem`}
                aria-current={isActive ? 'page' : undefined}
              >
                <div class="flex items-center justify-between">
                  <span>{link.label}</span>
                  {isActive && (
                    <svg class="w-5 h-5 text-cyan-500" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                  )}
                </div>
              </a>
            );
          })
        }

        <!-- Mobile Language Switcher - Click Modal -->
        <div class="px-4 pt-6 mt-4 border-t border-white/10">
          <button
            type="button"
            id="mobile-language-button"
            class="w-full flex items-center justify-between px-4 py-3 bg-slate-900/80 border-2 border-white/10 rounded-xl text-base font-bold text-white hover:border-cyan-500/50 transition-all duration-300"
            aria-label="Change language"
            aria-expanded="false"
          >
            <div class="flex items-center gap-3">
              <span class="text-xl">{currentLang === 'id' ? '🇮🇩' : '🇬🇧'}</span>
              <span>{currentLang === 'id' ? 'Bahasa Indonesia' : 'English'}</span>
            </div>
            <svg class="w-5 h-5 text-cyan-400 transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>

          <!-- Mobile Language Modal -->
          <div id="mobile-language-modal" class="hidden mt-3 w-full bg-slate-900/95 backdrop-blur-xl border-2 border-white/10 rounded-2xl shadow-2xl shadow-black/50 overflow-hidden transform transition-all duration-300">
            <!-- Modal Header -->
            <div class="px-4 py-3 border-b border-white/10 flex items-center justify-between">
              <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider">
                {currentLang === 'id' ? 'Pilih Bahasa' : 'Select Language'}
              </p>
              <button type="button" class="text-gray-400 hover:text-white transition-colors" aria-label="Close">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>

            <!-- Language Options -->
            <div class="p-2">
              <!-- Indonesian Option -->
              <a
                href={idUrl}
                class={`flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-300 ${
                  currentLang === 'id'
                    ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border border-cyan-500/30'
                    : 'hover:bg-white/5 border border-transparent'
                }`}
              >
                <span class="text-2xl">🇮🇩</span>
                <div class="flex-1">
                  <!-- <p class={`text-sm font-bold ${currentLang === 'id' ? 'text-cyan-400' : 'text-white'}`}>
                    Bahasa Indonesia
                  </p> -->
                  <p class="text-xs text-gray-400">ID</p>
                </div>
                {currentLang === 'id' && (
                  <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                  </svg>
                )}
              </a>

              <!-- English Option -->
              <a
                href={enUrl}
                class={`flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-300 ${
                  currentLang === 'en'
                    ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border border-cyan-500/30'
                    : 'hover:bg-white/5 border border-transparent'
                }`}
              >
                <span class="text-2xl">🇬🇧</span>
                <div class="flex-1">
                  <!-- <p class={`text-sm font-bold ${currentLang === 'en' ? 'text-cyan-400' : 'text-white'}`}>
                    English
                  </p> -->
                  <p class="text-xs text-gray-400">EN</p>
                </div>
                {currentLang === 'en' && (
                  <svg class="w-5 h-5 text-cyan-400" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                  </svg>
                )}
              </a>
            </div>
          </div>
        </div>

        <!-- Mobile CTA - WhatsApp -->
        <div class="px-4 mt-4">
          <a
            href={whatsappUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="group relative w-full inline-flex items-center justify-center rounded-xl
                   px-6 py-3 text-base font-bold
                   bg-gradient-to-r from-green-500 via-green-600 to-emerald-600
                   text-white shadow-lg shadow-green-500/25
                   transition-all duration-300
                   hover:shadow-xl hover:scale-105
                   overflow-hidden"
          >
            <div class="absolute inset-0 bg-gradient-to-r from-green-600 via-green-700 to-emerald-700 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>

            <span class="relative z-10 flex items-center justify-center gap-2">
              <!-- WhatsApp Icon -->
              <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
              </svg>
              {t.nav.consultation}
            </span>
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.5s ease-out forwards;
    opacity: 0;
  }

  @keyframes shimmer {
    100% {
      transform: translateX(100%);
    }
  }

  .animate-shimmer {
    animation: shimmer 2s infinite;
  }

  /* Scroll Effect - Add shadow when scrolled */
  #navbar.scrolled {
    box-shadow: 0 4px 20px rgba(6, 182, 212, 0.1);
  }

  /* Gradient Border Bottom */
  #navbar::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, rgba(6, 182, 212, 0.3) 0%, rgba(20, 184, 166, 0.3) 50%, rgba(6, 182, 212, 0.3) 100%);
  }

  /* Optional: Glow effect on scroll */
  #navbar.scrolled::after {
    background: linear-gradient(90deg, rgba(6, 182, 212, 0.5) 0%, rgba(20, 184, 166, 0.5) 50%, rgba(6, 182, 212, 0.5) 100%);
    box-shadow: 0 4px 30px rgba(6, 182, 212, 0.15);
  }

  /* Language Modal Hover Effect */
  .lang-switcher:hover .lang-modal {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    pointer-events: auto;
  }

  .lang-switcher:hover .lang-arrow {
    transform: rotate(180deg);
  }

  /* Prevent modal from closing when hovering over it */
  .lang-switcher .lang-modal {
    pointer-events: none;
  }

  /* Mobile Language Modal Animation */
  #mobile-language-modal {
    animation: slideDown 0.3s cubic-bezier(0.22, 1, 0.36, 1);
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Language option hover effects */
  .group\/item:hover {
    transform: translateX(4px);
  }
</style>

<!-- Enhanced Mobile Menu Toggle Script with Scroll Effect -->
<script>
  // Mobile menu toggle
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  const navbar = document.getElementById('navbar');

  mobileMenuButton?.addEventListener('click', () => {
    const isExpanded = mobileMenuButton?.getAttribute('aria-expanded') === 'true';
    mobileMenuButton?.setAttribute('aria-expanded', String(!isExpanded));
    mobileMenu?.classList.toggle('hidden');

    // Animate hamburger to X
    const svg = mobileMenuButton?.querySelector('svg');
    if (svg && !isExpanded) {
      svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />';
    } else {
      svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />';
    }
  });

  // Add scroll effect to navbar
  let lastScroll = 0;
  window.addEventListener('scroll', () => {
    const currentScroll = window.pageYOffset;

    if (currentScroll > 50) {
      navbar?.classList.add('scrolled');
    } else {
      navbar?.classList.remove('scrolled');
    }

    lastScroll = currentScroll;
  });

  // Close mobile menu when clicking outside
  document.addEventListener('click', (e) => {
    if (!mobileMenuButton?.contains(e.target) && !mobileMenu?.contains(e.target)) {
      const isExpanded = mobileMenuButton?.getAttribute('aria-expanded') === 'true';
      if (isExpanded) {
        mobileMenuButton?.setAttribute('aria-expanded', 'false');
        mobileMenu?.classList.add('hidden');

        // Reset hamburger icon
        const svg = mobileMenuButton?.querySelector('svg');
        if (svg) {
          svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />';
        }
      }
    }
  });

  // Close mobile menu on window resize
  window.addEventListener('resize', () => {
    if (window.innerWidth >= 1024) {
      mobileMenu?.classList.add('hidden');
      mobileMenuButton?.setAttribute('aria-expanded', 'false');

      // Reset hamburger icon
      const svg = mobileMenuButton?.querySelector('svg');
      if (svg) {
        svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />';
      }
    }
  });

  // Mobile Language Modal Toggle
  const mobileLanguageButton = document.getElementById('mobile-language-button');
  const mobileLanguageModal = document.getElementById('mobile-language-modal');

  mobileLanguageButton?.addEventListener('click', () => {
    const isExpanded = mobileLanguageButton.getAttribute('aria-expanded') === 'true';
    mobileLanguageButton.setAttribute('aria-expanded', String(!isExpanded));
    mobileLanguageModal?.classList.toggle('hidden');

    // Rotate arrow
    const arrow = mobileLanguageButton.querySelector('svg');
    if (arrow) {
      arrow.style.transform = isExpanded ? 'rotate(0deg)' : 'rotate(180deg)';
    }
  });

  // Close mobile language modal when clicking outside
  document.addEventListener('click', (e) => {
    if (!mobileLanguageButton?.contains(e.target) && !mobileLanguageModal?.contains(e.target)) {
      const isExpanded = mobileLanguageButton?.getAttribute('aria-expanded') === 'true';
      if (isExpanded) {
        mobileLanguageButton?.setAttribute('aria-expanded', 'false');
        mobileLanguageModal?.classList.add('hidden');

        const arrow = mobileLanguageButton?.querySelector('svg');
        if (arrow) {
          arrow.style.transform = 'rotate(0deg)';
        }
      }
    }
  });

  // Add smooth transition on language switch
  document.querySelectorAll('.lang-modal a, #mobile-language-modal a').forEach(link => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const href = link.getAttribute('href');

      // Fade out animation
      document.body.style.opacity = '0.7';
      document.body.style.transition = 'opacity 0.2s ease';

      setTimeout(() => {
        if (href) {
          window.location.href = href;
        }
      }, 200);
    });
  });
</script>
